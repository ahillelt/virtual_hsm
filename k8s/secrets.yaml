# This is a template file - DO NOT commit actual secrets to version control
# Use kubectl create secret or external secret management tools

# Example: Creating secrets via kubectl
# kubectl create secret generic vhsm-secrets \
#   --from-literal=master-key=$(openssl rand -hex 32) \
#   --from-literal=admin-password=$(openssl rand -base64 32) \
#   --namespace=vhsm

apiVersion: v1
kind: Secret
metadata:
  name: vhsm-secrets
  namespace: vhsm
  labels:
    app: vhsm
type: Opaque
stringData:
  # SECURITY WARNING: Replace these with actual secure values
  # Use external secret management in production (Vault, AWS Secrets Manager, etc.)
  master-key: "REPLACE_WITH_SECURE_RANDOM_KEY"
  admin-password: "REPLACE_WITH_SECURE_ADMIN_PASSWORD"
